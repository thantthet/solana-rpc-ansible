---
- name: Gather variables on basis of solana network
  include_vars: "{{ item }}"
  with_first_found:
    - "{{ solana_network | lower }}.yml"
    - "{{ solana_network | lower }}-default.yml"

# Install dependencies
- import_tasks: dependencies.yml
  tags:
    - solana_install


# Set up a solana user
- import_tasks: user.yml
  tags:
    - solana_configure
    - solana_install
    - solana_run

# Install  rust
- import_tasks: rust.yml
  when: install_rust is defined and install_rust
  tags:
    - solana_install

# Install solana
- import_tasks: install.yml
  tags:
    - solana_install


# Set cpu optimisations
- import_tasks: optimisations.yml
  tags:
    - solana_configure

# Setup RPC identity
- import_tasks: identity.yml
  tags:
    - solana_configure

# Setup bigtable
- import_tasks: bigtable.yml
  tags:
    - solana_configure


# Setup solana service files
- import_tasks: service.yml
  tags:
    - solana_run
